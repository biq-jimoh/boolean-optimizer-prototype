# Lambda-optimized requirements for Bankruptcy Query Optimizer
# This file excludes heavy dependencies not needed for Lambda deployment

# Core dependency
openai-agents>=0.2.0

# Data validation
pydantic>=2.0.0

# Web search and content extraction (lightweight)
httpx>=0.25.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Environment variable support
python-dotenv>=1.0.0

# Note: Playwright is excluded as it's too heavy for Lambda
# and not essential for the core functionality